[project]
name = "ice_storm-weather-station"
version = "0.1.0"
description = "IceStorm weather demo station application"
requires-python = ">=3.12"
dependencies = ["zeroc-ice"]

[dependency-groups]
lint = [
    "pyright>=1.1.407",
    "ruff>=0.14.5",
]
