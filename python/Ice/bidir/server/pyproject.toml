[project]
name = "ice-bidir-server"
version = "0.1.0"
description = "Ice bidir demo server application"
requires-python = ">=3.12"
dependencies = ["zeroc-ice", "common"]

[tool.uv.sources]
common = { path = "../../../common" }

[dependency-groups]
lint = [
    "pyright>=1.1.407",
    "ruff>=0.14.5",
]
