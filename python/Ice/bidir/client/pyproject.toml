[project]
name = "ice-bidir-client"
version = "0.1.0"
description = "Ice bidir demo client application"
requires-python = ">=3.12"
dependencies = ["zeroc-ice", "common"]

[dependency-groups]
lint = [
    "pyright>=1.1.407",
    "ruff>=0.14.5",
]
