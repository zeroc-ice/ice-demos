[project]
name = "client"
version = "0.1.0"
description = "Ice greeter demo async dispatcher server application"
requires-python = ">=3.12"
dependencies = ["zeroc-ice"]

[dependency-groups]
lint = [
    "pyright>=1.1.407",
    "ruff>=0.14.5",
]
