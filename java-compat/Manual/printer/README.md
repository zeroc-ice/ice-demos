This demo implements the printer example in Ice Manual.
To run it, start the server in a window:

```
java -jar build/libs/server.jar
```

In a separate window, run the client:

```
java -jar build/libs/client.jar
```
