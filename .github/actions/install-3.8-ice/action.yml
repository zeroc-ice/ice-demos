name: Instal Nightly Ice

runs:
  using: "composite"
  steps:
    - name: Install Ice Brew Formula
      if: runner.os == 'macOS'
      shell: bash
      run: |
        brew install zeroc-ice/tap/ice

    - name: Install Ice Linux Repository
      if: runner.os == 'Linux'
      shell: bash
      run: |
        wget https://download.zeroc.com/ice/3.8/ubuntu24.04/ice-repo-3.8_1.0_all.deb -O ice-repo-3.8.deb
        sudo dpkg -i ice-repo-3.8.deb
        rm ice-repo-3.8.deb

        sudo apt-get update
        sudo apt-get install -y libzeroc-ice-dev
